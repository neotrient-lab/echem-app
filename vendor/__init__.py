# vendor/ holds third-party libraries we ship alongside the app so the
# PyInstaller bundle is fully self-contained.  The launcher prepends
# this folder to sys.path before importing the rest of the app.
